@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LogiT Shop</title>

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa;
            font-family: "Segoe UI", sans-serif;
        }

        /* ===== FORM NGANG ===== */
        .login-section {
            min-height: 80vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .login-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
            width: 80%;
            max-width: 1000px;
            display: flex;
            overflow: hidden;
        }

        .login-image {
            flex: 1;
            background: url('https://images.unsplash.com/photo-1593642532973-d31b6557fa68?auto=format&fit=crop&w=900&q=80') center;
        }

        .login-form {
            flex: 1;
            padding: 50px;
        }

            .login-form h2 {
                color: #dc3545;
                font-weight: 700;
                margin-bottom: 30px;
            }

            .login-form .form-control {
                border-radius: 8px;
            }

            .login-form .btn {
                border-radius: 8px;
                font-weight: 600;
            }

        /* ===== FOOTER ===== */
        footer {
            background: #212529;
            color: #fff;
            padding: 60px 0 20px;
        }

            footer h5 {
                color: #ffc107;
                font-weight: 600;
            }

            footer a {
                text-decoration: none;
                color: #ddd;
            }

                footer a:hover {
                    color: #fff;
                }

        .footer-bottom {
            border-top: 1px solid rgba(255,255,255,0.2);
            margin-top: 30px;
            padding-top: 15px;
            text-align: center;
            color: #aaa;
        }
    </style>
</head>

<body>

    <!-- ===== NAVBAR ===== -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-danger d-flex align-items-center" href="@Url.Action("Index", "Home")">
                <img style="height: 45px; pointer-events: none;" class="me-2"
                     src="@Url.Content("~/Content/IMG/logo.png")" alt="logo" />
                <span class="fs-4 fw-bold">LogiT</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 fw-semibold">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">TRANG CHỦ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#footer-contact">LIÊN HỆ</a>
                    </li>
                </ul>

                <form class="d-flex">
                    <input class="form-control me-2" type="search" placeholder="Search...">
                    <button class="btn btn-outline-dark" type="submit">
                        <i class="bi bi-search"></i>
                    </button>
                </form>

                <div class="ms-3 d-flex align-items-center">
                    <span class="text-muted small me-2">admin@gmail.com</span>
                    <i class="bi bi-person-circle fs-5"></i>
                </div>
            </div>
        </div>
    </nav>

    <!-- ===== FORM NGANG ===== -->
    <section class="login-section">
        <div class="login-container">
            <div class="login-image"></div>

            <div class="login-form">
                <h2>Đăng Nhập</h2>
                <form method="post" action="@Url.Action("DangNhap", "Account")">
                    <div class="mb-3">
                        <label for="Email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="Email" name="Email" placeholder="Nhập email của bạn" required>
                    </div>
                    <div class="mb-3">
                        <label for="Password" class="form-label">Mật khẩu</label>
                        <input type="password" class="form-control" id="Password" name="Password" placeholder="Nhập mật khẩu" required>
                    </div>
                    <div class="mb-3 form-check">
                        <input type="checkbox" class="form-check-input" id="RememberMe">
                        <label class="form-check-label" for="RememberMe">Ghi nhớ đăng nhập</label>
                    </div>
                    <button type="submit" class="btn btn-danger w-100 py-2">Đăng nhập</button>
                    <div class="text-center mt-3">
                        <small>Bạn chưa có tài khoản? <a href="@Url.Action("DangKy", "Account")" class="text-danger fw-semibold">Đăng ký ngay</a></small>
                    </div>
                </form>
            </div>
        </div>
    </section>

    <!-- ===== FOOTER ===== -->
    <footer id="footer-contact">
        <div class="container">
            <div class="row gy-4">
                <div class="col-md-4">
                    <h5>Về LogiT</h5>
                    <p>Chuyên cung cấp linh kiện máy tính, laptop, phụ kiện chính hãng. Chúng tôi luôn nỗ lực mang đến trải nghiệm mua sắm tốt nhất cho khách hàng.</p>
                </div>
                <div class="col-md-4">
                    <h5>Liên hệ</h5>
                    <ul class="list-unstyled">
                        <li><i class="bi bi-geo-alt"></i> 123 Nguyễn Trãi, TP. HCM</li>
                        <li><i class="bi bi-telephone"></i> 0123 456 789</li>
                        <li><i class="bi bi-envelope"></i> support@logit.vn</li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5>Theo dõi chúng tôi</h5>
                    <a href="#" class="me-3"><i class="bi bi-facebook fs-4"></i></a>
                    <a href="#" class="me-3"><i class="bi bi-instagram fs-4"></i></a>
                    <a href="#"><i class="bi bi-youtube fs-4"></i></a>
                </div>
            </div>

            <div class="footer-bottom mt-4">
                © 2025 LogiT Shop. All Rights Reserved.
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
